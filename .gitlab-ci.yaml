image: node:12.13.0

cache:
  paths:
    - node_modules/


before_script:
  - npm install

pages:
  stage: deploy
  script:
    - rm -rf public
    - cd ./site
    - CI=false npm run build
    - mv ./build ../public
  artifacts:
    paths:
      - public
  only:
    - master
