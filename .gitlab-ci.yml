image: node:20.18.0

variables:
  PHASE: PRODUCTION

# compile_resume:
#   stage: build
#   image: timnn/texlive # use a Docker image for LaTeX from https://hub.docker.com/
#   script:
#     # build the pdf
#     - ls -la
#     - pdflatex ./public/resume/JonoAugustineResume.tex
#     # copy to images
#     - mv ./public/resume/JonoAugustineResume.pdf ./public/images
#   artifacts:
#     paths:
#       - ./public/images/JonoAugustineResume.pdf
#   only:
#     - master

pages:
  stage: deploy
  script:
    - cd ./site
    - npm install
    - npm run build
  artifacts:
    paths:
      - public
  only:
    refs:
      - master
    changes:
      - site/**/*
